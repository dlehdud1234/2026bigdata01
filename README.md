# 수업
git merge 해당 브랜치명
git branch
git checkout -b 브랜치명
git switch -c 브랜치명
git push origin --delete 브랜치명
깃허브(리눅스코드 포함) 60 코드 40
가장 최근 커밋 메세지 수정 --amend
git log online / graph 삭제 옵션 -d
array.t
arr.dtype
numpy 배열, 속성, 연산, 제공 통계 함수(git참고)
